_yabsnap_completions() {
    # Path where yabsnap.sh is also present.
    cd "/usr/share/yabsnap"
    COMPREPLY=( $(python -O -u -m code.shell_completions "${COMP_WORDS[@]}") )
    return 0
}

complete -F _yabsnap_completions yabsnap
