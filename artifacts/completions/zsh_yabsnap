#compdef yabsnap

_yabsnap() {
  # Path where yabsnap.sh is also present.
  local script_dir="/usr/share/yabsnap"
  local output=$(STYLE=zsh PYTHONPATH=$script_dir python -O -u -m code.shell_completions "${words[@]}")
  eval "$output"
}

compdef _yabsnap yabsnap
